% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_cluster.R
\name{plot_clusters}
\alias{plot_clusters}
\title{Plot the heatmaps of REVOLVER"s clusters.}
\usage{
plot_clusters(
  x,
  cluster_palette = distinct_palette_few,
  cutoff_drivers = 5,
  cutoff_trajectories = 4,
  arrow.symbol = " → "
)
}
\arguments{
\item{x}{A \code{REVOLVER} object with fits and clusters.}

\item{cutoff_drivers}{Plot only drivers that occur in at least \code{cutoff_drivers} patients.}

\item{cutoff_trajectories}{Plot only trajectories that occur in at least \code{cutoff_trajectories} patients.}

\item{arrow.symbol}{UNICODE code to display arrows. Saving to PDF outputs with standard
methods (ggsave, cairo, etc), often can lead to errors with UNICODE chars; therefore either print to PNG or change
this variable to, e.g.,  \code{" --> "} to render arrows properly.}

\item{clusters_palette}{A palette function that should return the colour of
an arbitrary number of clusters.}
}
\value{
A \code{ggplot} plot.
}
\description{
Plot the heatmaps of REVOLVER"s clusters, as tiles.

The top tile is patients vs trajectories, and bottom is patients vs drivers.
For drivers colours reflect mean CCF/ binary values of a driver in every patient, and
clonality status. For trajectories colours reflect if they are initiating or progressing,
depending on the present of GL in the trajectory.

Patients are sorted by cluster to match the dendrogram that one can obtain with \code{\link{plot_dendrogram}}.
}
\examples{
# Data released in the 'evoverse.datasets'
data('TRACERx_NEJM_2017_REVOLVER', package = 'evoverse.datasets')

plot_clusters(TRACERx_NEJM_2017_REVOLVER)

plot_clusters(TRACERx_NEJM_2017_REVOLVER, cutoff_drivers = 10, cutoff_trajectories = 3)
}
\seealso{
Other Plotting functions: 
\code{\link{distinct_palette_few}()},
\code{\link{distinct_palette_many}()},
\code{\link{gradient_palette}()},
\code{\link{plot_DET_index}()},
\code{\link{plot_dendrogram}()},
\code{\link{plot_drivers_clonality}()},
\code{\link{plot_drivers_graph}()},
\code{\link{plot_drivers_occurrence}()},
\code{\link{plot_jackknife_cluster_stability}()},
\code{\link{plot_jackknife_coclustering}()},
\code{\link{plot_jackknife_trajectories_stability}()},
\code{\link{plot_patient_CCF_histogram}()},
\code{\link{plot_patient_data}()},
\code{\link{plot_patient_mutation_burden}()},
\code{\link{plot_patient_oncoprint}()},
\code{\link{plot_patient_trees_scores}()}
}
\concept{Plotting functions}
